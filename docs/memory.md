# Project memory policy

Memory is optional context, not a source of truth or a startup requirement. Use it only when the task calls for it or GitHub and relevant repository files cannot recover necessary prior context.

Do not store secrets, access data, private configuration, full documents, or sensitive logs. Keep any useful durable entry short and linked to its GitHub/repository source.
